import cat.udl.eps.is.StdLib.*
val words = List("patata", "brava", "en", "salsa")

countLengths(words)

val empty_list = List[String]()

countLengths(empty_list)

val base = 2

val exponent = 5

sumFirstPowersOf(base, exponent)

scala.math.pow(2, 4).toInt

